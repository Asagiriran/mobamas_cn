b.js 的61行从'src': window.image_server + '?url=' + encodeURIComponent(window.base_url + _img_path),改为'src': window.image_server + _img_path,

a.js的image_server = "../../../../"

voice_manifest的路径全修改

h5后编, 服务器根目录必须得在imas文件夹里.